// Rota só para reservar o lugar do botão "+" na barra de abas.
// O botão em si (tabBarButton) está no _layout.tsx e abre /nova-denuncia.
export default function NovaPlaceholder() {
  return null;
}
